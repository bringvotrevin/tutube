mixin video(video)
	a(href=`/video/${video._id}`) #{video.title}
	ul
		li description: #{video.description}
		li createdAt: #{video.createdAt}
		li hashTags: #{video.hashTags.join()}
		li Views: #{video.meta.views}
		li Rating: #{video.meta.rating}
	